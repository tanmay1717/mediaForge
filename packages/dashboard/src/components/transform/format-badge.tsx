'use client';
import React from 'react';

interface FormatBadgeProps { format: string; }

/**
 * TODO: Small badge showing the negotiated output format
 * - Color coded: AVIF=green, WebP=blue, JPEG=amber, PNG=purple
 * - Tooltip explaining why this format was chosen
 */
export default function FormatBadge(props: FormatBadgeProps) {
  return (
    <div data-component="FormatBadge">
      {/* TODO: Implement FormatBadge */}
    </div>
  );
}
