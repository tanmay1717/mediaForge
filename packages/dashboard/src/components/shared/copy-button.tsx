'use client';
import React from 'react';

interface CopyButtonProps { text: string; className?: string; }

/**
 * TODO: Click-to-copy button with tooltip feedback
 * - Click → copy text to clipboard
 * - Show 'Copied!' tooltip for 2 seconds
 * - Icon changes from clipboard to checkmark on copy
 */
export default function CopyButton(props: CopyButtonProps) {
  return (
    <div data-component="CopyButton">
      {/* TODO: Implement CopyButton */}
    </div>
  );
}
