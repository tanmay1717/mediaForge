'use client';
import React from 'react';

interface RelativeTimeProps { date: string; }

/**
 * TODO: Relative time display ('2 hours ago')
 * - Calculate relative to now
 * - Update every minute (useEffect interval)
 * - Tooltip shows full date on hover
 */
export default function RelativeTime(props: RelativeTimeProps) {
  return (
    <div data-component="RelativeTime">
      {/* TODO: Implement RelativeTime */}
    </div>
  );
}
