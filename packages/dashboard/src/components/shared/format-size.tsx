'use client';
import React from 'react';

interface FormatSizeProps { bytes: number; }

/**
 * TODO: Display human-readable file size
 * - Use formatBytes() from core
 * - Render as inline <span>
 */
export default function FormatSize(props: FormatSizeProps) {
  return (
    <div data-component="FormatSize">
      {/* TODO: Implement FormatSize */}
    </div>
  );
}
