'use client';
import React from 'react';



/**
 * TODO: Table of API keys
 * - Columns: label, prefix (mf_live_...), status, last used, created, actions
 * - Revoke button with confirmation
 * - Copy prefix button
 * - Use useApiKeys() hook
 */
export default function ApiKeyTable() {
  return (
    <div data-component="ApiKeyTable">
      {/* TODO: Implement ApiKeyTable */}
    </div>
  );
}
