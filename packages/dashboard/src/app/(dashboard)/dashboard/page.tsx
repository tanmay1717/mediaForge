'use client';
import React from 'react';

/**
 * TODO: Dashboard home page
 * - Stats cards row: total assets, storage used, bandwidth, transforms
 * - Recent uploads list (last 10 assets)
 * - Quick upload drop zone
 * - Use useStats() hook for data
 */
export default function DashboardPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold mb-6">Dashboard</h1>
      {/* TODO: Stats cards, recent uploads, quick upload */}
      <p className="text-gray-500">TODO: Implement dashboard home</p>
    </div>
  );
}
