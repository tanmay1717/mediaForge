'use client';
import React from 'react';

/**
 * TODO: Upload page
 * - Large drag-and-drop zone (UploadZone component)
 * - Upload from URL input (UploadFromUrl component)
 * - Folder picker for destination
 * - Upload queue with per-file progress bars
 * - Multi-file support (up to 20 files)
 */
export default function UploadPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold mb-6">Upload</h1>
      {/* TODO: Implement upload zone + progress */}
      <p className="text-gray-500">TODO: Implement upload page</p>
    </div>
  );
}
