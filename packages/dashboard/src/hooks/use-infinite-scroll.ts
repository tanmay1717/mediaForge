import { useState, useEffect, useRef, useCallback } from 'react';

/**
 * TODO: Implement use-infinite-scroll
 *  echo "- Accept a value and delay (ms), return debounced value";;
  use-clipboard) echo "- Accept text, copy to navigator.clipboard, return { copied, copy }";;
  use-infinite-scroll) echo "- Use IntersectionObserver on a sentinel element, call onLoadMore";;
  use-keyboard-shortcut) echo "- Listen for keydown events matching a shortcut, call handler";;
esac)
 */
export function useinfinitescroll() {
  // TODO: Implement
  return {};
}
