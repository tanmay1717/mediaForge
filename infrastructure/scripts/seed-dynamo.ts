/**
 * Seed DynamoDB with test data (dev environment only).
 *
 * TODO:
 * - Create a test user record in the Users table
 * - Create a root folder for the test user
 * - Create 2-3 child folders
 * - Create 5-10 sample asset records (no actual S3 files needed for testing)
 * - Create a test API key
 *
 * Run: npx ts-node infrastructure/scripts/seed-dynamo.ts
 */
console.log('TODO: Implement DynamoDB seeding script');
console.log('This should only run against the dev environment');
